export async function summarizeWeeklyReport(_body: {
  wins: string;
  pains: string;
  nextGoals: string;
}): Promise<string> {
  // Week 7 task #21: wire to AI Gateway with structured-output JSON schema.
  return "(summary placeholder — Week 7)";
}
