// Placeholder for Week 2: KnowledgeRetrieval module.
// Will combine pgvector search over annotations + deterministic slug lookup.

export async function retrieveKnowledge(_args: {
  tenantId: string;
  query: string;
  topK: number;
}): Promise<string> {
  return "(knowledge retrieval not yet implemented — Week 2)";
}
